# ============================================================
# PRE-LAUNCH. THE WHOLE SITE IS CLOSED TO CRAWLERS ON PURPOSE.
# ------------------------------------------------------------
# Two facts make this the right posture today, and both stop being
# true on the same day:
#
#   1. NOTHING IS PUBLISHED. 54 merchants are registered and none has
#      cleared the capture gate, so every room says "nothing is
#      stocked yet". Letting Google index that costs us the first
#      impression on every one of these URLs to sell an empty shop.
#
#   2. THE DOMAINS LANDED, so this reason is now half spent.
#      verdacultivation.store is canonical and verdastudio.store
#      308s to it in vercel.json, which is the duplicate-content
#      problem this clause was written to avoid, solved properly.
#      What is left is reason 1: most rooms are still empty, and an
#      indexed empty room spends the first impression on that URL to
#      sell nothing.
#
#      SO THIS FILE IS NOW THE ONLY THING HOLDING BACK INDEXING, and
#      it should be opened room by room as they stock rather than all
#      at once. Four rooms have inventory today: play, tabletop,
#      battlestation and workshop. Opening those and the homepage is
#      a deliberate decision about a live site, so it is left to the
#      owner rather than slipped in with a domain change.
#
# Blocking now costs nothing, because there is nothing to index.
#
# ------------------------------------------------------------
# HOW TO OPEN IT, WHICH IS THE PART THAT GETS FORGOTTEN
# ------------------------------------------------------------
# When a real domain is live AND at least one merchant is published:
#
#   1. Replace everything below with the LAUNCH block at the bottom.
#   2. Set SITE_URL to the real domain and run `npm run sitemap`.
#   3. Check /sitemap.xml lists what you expect and nothing noindex.
#
# A `Disallow: /` nobody remembers to remove is a site that quietly
# never gets traffic, and the symptom — "our SEO isn't working" —
# points nowhere near this file. It is in CLAUDE.md's launch
# checklist for that reason.
# ============================================================

User-agent: *
Disallow: /

# ============================================================
# THE LAUNCH BLOCK — paste this over everything above.
# ------------------------------------------------------------
# User-agent: *
# Allow: /
#
# # /collect is an operator tool. /api costs a live scrape of every
# # shop when it is crawled — Kawaii Katz's guide is blunt about that
# # and it is the same engine here.
# Disallow: /collect
# Disallow: /api/
#
# Sitemap: https://REAL-DOMAIN-HERE/sitemap.xml
# ============================================================
